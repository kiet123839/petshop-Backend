server:
  port: ${SERVER_PORT:8080}

spring:
  application:
    name: petshop-auth

  datasource:
    url: ${DB_URL:jdbc:sqlserver://localhost:1433;databaseName=PETSHOP_DB;encrypt=false;trustServerCertificate=true}
    username: ${DB_USERNAME:sa}
    password: ${DB_PASSWORD:Tumi*0710}
    driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver

  jpa:
    database-platform: org.hibernate.dialect.SQLServerDialect
    hibernate:
      ddl-auto: none
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  mail:
    host: smtp.gmail.com
    port: 587
    username: khoautcit27@gmail.com
    password: twpf ylfg ewpq buqc
    properties:
      "[mail.smtp.auth]": true
      "[mail.smtp.starttls.enable]": true
      "[mail.smtp.connectiontimeout]": 5000
      "[mail.smtp.timeout]": 3000
      "[mail.smtp.writetimeout]": 5000

jwt:
  secret: petshop-super-secret-key-2024-very-long-and-secure-do-not-share
  expiration: 86400000

notifications:
  email:
    enabled: true
    from: khoautcit27@gmail.com
    from-name: PETSHOP

vnpay:
  pay-url: ${VNPAY_PAY_URL:https://sandbox.vnpayment.vn/paymentv2/vpcpay.html}
  tmn-code: ${VNPAY_TMN_CODE:GLWLGTLQ}
  hash-secret: ${VNPAY_HASH_SECRET:RQPC1FZ2T7ST646V96F0B99RLJM3BMFW}
  return-url: ${VNPAY_RETURN_URL:http://localhost:8080/api/payments/vnpay/return}
  frontend-return-url: ${VNPAY_FRONTEND_RETURN_URL:http://localhost:5500/petshop-frontend/payments_use.html}
  expire-minutes: ${VNPAY_EXPIRE_MINUTES:15}
